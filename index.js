import { newDropDownMenu } from "simpledrop-js/index.js";

const btn = document.getElementsByClassName("menu");
const dropdown = document.getElementsByClassName("dropdown");

newDropDownMenu(btn, dropdown);
